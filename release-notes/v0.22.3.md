# Phantty v0.22.3

## Improvements

- Expanded the default `web` / `browser` agent skill with GenericAgent-inspired
  browser workflow guidance and reusable JavaScript helpers for compact page
  snapshots, action deltas, and form value updates.

## Fixes

- Fixed DeepSeek thinking-mode retries after skill/tool preloads by ensuring
  assistant tool-call history includes `reasoning_content` when required by the
  API.

